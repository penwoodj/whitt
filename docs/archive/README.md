# Archive

Zero-loss archive of stray drafts found at repo root during 2026-08-14 refactor.

- `sample-projects-root-draft/` — divergent untracked draft of sample project data
  (written to root `src/` by mistaken agent run; canonical copy lives at
  `vision-graph-ui/src/data/sample-projects/`, referenced by `fsGraphLoader.ts`).
